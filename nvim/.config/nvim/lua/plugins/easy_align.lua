return {
    'junegunn/vim-easy-align',
    config = function()
        -- Add other configurations here
    end,
    -- It's not working here ... must be in global keys file
--    keys = {
--        { "ga", mode = { "n", "x" }, desc = "Start EasyAlign" }
--    }

}
